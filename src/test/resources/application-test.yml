spring:
  sql:
    init:
      mode: always

  jpa:
    hibernate:
      ddl-auto: none

  data:
    redis:
      repositories:
        enabled: false

  kafka:
    listener:
      auto-startup: false

jwt:
  secret: test-secret-key-must-be-at-least-256-bits-long-for-hs256-algorithm-test
  expiration: 3600000

outbox:
  relay:
    enabled: false
